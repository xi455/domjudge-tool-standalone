version: '3.3'

services:
  app:
    build: .
    container_name: domjudge_tool_standalone
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - standalone-log:/app/log

volumes:
  standalone-log: